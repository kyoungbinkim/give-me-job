# give-me-job environment variables
#
# Job-source integrations are a TODO. The jobkorea / saramin / work24 adapters
# were removed, so no API keys are required at the moment.
#
# When a job source is added back via tools/fetch-jobs.mjs, document its
# required keys here.
